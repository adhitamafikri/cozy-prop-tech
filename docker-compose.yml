services:
  api:
    build:
      context: ./backend/api
      dockerfile: Dockerfile
      target: development
    ports:
      - "8082:8082"
    volumes:
      - ./backend/api:/app
    env_file:
      - ./backend/api/.env
    networks:
      - cozy-network

networks:
  cozy-network:
    driver: bridge
